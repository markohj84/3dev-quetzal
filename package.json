{
  "name": "quetzal",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "prebuild": "npm run check:boundary",
    "build": "next build",
    "start": "next start",
    "check:boundary": "! grep -rilE '3dev|quetzal' core/ || (echo 'core/ references a client' && exit 1)"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.0",
    "@upstash/redis": "^1.34.0",
    "next": "^15.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "typescript": "^5.6.0"
  }
}
